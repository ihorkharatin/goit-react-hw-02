import clsx from "clsx";
import s from "./Notification.module.css";

const Notification = () => {
  return <div className={clsx(s.notification)}>No feedback yet</div>;
};

export default Notification;
